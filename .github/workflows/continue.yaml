name: Continue Workflow

on:
  workflow_run:
    workflows: ["Approval Step"]
    types:
      - completed

jobs:
  continue:
    runs-on: ubuntu-latest
    steps:
      - name: Continue if approved
        run: echo "Step is approved. Continuing..."
